# AI Patcher Catalog

Remote catalog for generated AI Patcher packages.

This repository stores generated world-content packages outside the `AI-Patcher` module repository. Worlds with `AI Patcher` enabled can read `index.json`, list available packages, download a package, import its assets into the current world, and apply the patch.
